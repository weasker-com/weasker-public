export type questionResult = {
  badge: {
    name: string;
    slug: string;
    image: string;
  };
  interview: {
    interviewQuestion: {
      text: string;
      slug: string;
      image: string;
    };
  };
};
